machine:
  ruby:
    version: 2.3.0
  environment:
    KNAPSACK_REPO_PATH: ~/gems/knapsack
dependencies:
  pre:
    # fix bug: https://discuss.circleci.com/t/bundler-fails-to-find-appropriate-version-despite-installing-appropriate-version-earlier-in-the-build/2815/36
    - bundler -v && gem install bundler -v 1.11.2 && bundler -v

    - mkdir ~/gems
    - cd ~/gems && git clone -b $CIRCLE_BRANCH --single-branch git@github.com:ArturT/knapsack.git
    - cd ~/gems/knapsack && git branch
    - cd ~/gems/knapsack && git log -n 1 | more
test:
  override:
    #- KNAPSACK_GENERATE_REPORT=true bundle exec rspec spec
    - bundle exec rake knapsack:rspec:
        parallel: true

    #- KNAPSACK_GENERATE_REPORT=true bundle exec cucumber features
    - bundle exec rake knapsack:cucumber:
        parallel: true

    #- KNAPSACK_GENERATE_REPORT=true bundle exec rake test
    - bundle exec rake knapsack:minitest[--verbose]:
        parallel: true
